export enum MessageDomain {
    SCREENSHOT = 'SCREENSHOT',
    VIDEO = 'VIDEO',
    UI = 'UI', // For general UI interactions not tied to specific capture
    INTERNAL = 'INTERNAL', // For internal state/checks
}
